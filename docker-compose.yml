version: "3.1"
services:
  lms-app:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: lms_app_container
    # image: a7medabdo6/care_managment
    volumes:
      - ./src:/App/src:ro
      - ~/hestuploads:/lms_app/uploads
    ports:
      - "8753:8753"